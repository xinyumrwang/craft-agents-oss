# 对标诊断交付契约

- 预览：`data/14-对标诊断预览.md`，包含证据对齐、总判断、分层评分、观察矩阵、可借鉴、不建议迁移、迁移风险、差异化机会、问题优先级和后续工作流。
- 状态：`data/14-对标诊断状态.json`，保存自有/参考图映射、比较重点、可比性、稳定观察/问题 ID、评分、建议、置信度、假设、版本和本轮变化。
- 每项迁移建议必须引用具体视觉证据并标明风险等级。写入后立即渲染真实绝对路径的 `markdown-preview`；不可用时内联全文。
