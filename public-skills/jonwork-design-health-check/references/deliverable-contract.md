# 方案评估交付契约

- 预览：`data/13-方案评估预览.md`，包含证据范围、总判断、分层评分、问题优先级、指导建议、后续工作流和验证计划。
- 状态：`data/13-方案评估状态.json`，保存输入图、上下文、证据、评分、稳定问题 ID、建议、通过标准、置信度、假设、版本和本轮变化。
- 每个评分必须关联依据；每个高优先级问题必须有可验收动作。预览写入后立即渲染 `markdown-preview`，不可用时内联全文。
