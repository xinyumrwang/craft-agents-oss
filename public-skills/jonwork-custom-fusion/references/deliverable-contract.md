# 自定义模式交付契约

- 图片：`data/04-自定义模式/vN/custom_fusion_1.png` 起，数量与 `count` 一致。
- 状态：`data/04-自定义模式/状态.json`，保存图号映射、原始要求、规范化约束、比例、数量、质量、来源、版本、生成状态、输出路径和本轮变化。
- 预览：`data/04-自定义模式/结果.md`，展示输入缩略图、图号映射、生成参数、输出图片和差异说明。
- 只有实际存在的图片可列为输出；失败项记录错误与可重试范围。修订创建 `vN+1`，不覆盖已确认版本。
